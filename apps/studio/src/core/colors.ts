// The chart/overlay palette — one source of truth for the few non-themed
// colours used in the SVG chart and the editor overlays. (Ant Design owns the
// surrounding UI chrome via its theme; these are just the drawing colours.)
export const INK = '#21201c';     // stitch ink
export const ORIGIN = '#5cb3ff';  // origin highlight — where a stitch comes from
export const SPACE = '#e8830c';   // spaces / bases — where a stitch is worked into
export const NEXT = '#a259ff';    // the "next" stitch while inserting between
export const SELECT = '#2f7bff';  // selection ring + head marker
export const GHOST = '#2f7bff';   // marquee box outline
