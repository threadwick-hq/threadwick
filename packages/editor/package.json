{
	"name": "@threadwick/editor",
	"version": "0.1.0",
	"description": "Threadwick's chart-editor core: the framework-agnostic, DOM-free model, renderer, connectivity, symbols, and read primitives that power the Studio editor and the Follow view.",
	"type": "module",
	"license": "AGPL-3.0-or-later",
	"sideEffects": false,
	"exports": {
		".": "./src/index.ts",
		"./browser": "./src/browser.ts"
	},
	"scripts": {
		"typecheck": "tsc --noEmit",
		"lint": "biome check .",
		"test": "vitest run"
	},
	"dependencies": {
		"@threadwick/types": "workspace:*",
		"qrcode": "^1.5.4"
	},
	"devDependencies": {
		"@types/node": "^20.14.0",
		"@types/qrcode": "^1.5.6",
		"typescript": "~5.6.3",
		"vitest": "^3.2.6"
	}
}
