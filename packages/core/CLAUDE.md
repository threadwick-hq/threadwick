# CLAUDE.md — @threadwick/core

The design-system rules are tool-agnostic and live in `packages/core/AGENTS.md`. Claude Code reads
them via the import below; this file loads automatically when you work under `packages/core/`.

@AGENTS.md
