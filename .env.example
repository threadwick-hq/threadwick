# URL the "Open Studio" CTA points to.
#
# Defaults to "/studio" (served by the Vercel rewrite in vercel.json) in every
# environment. "/studio" only resolves where that rewrite exists.
#
# Set this to override (e.g. point at a local Studio dev server):
# VITE_STUDIO_URL=http://localhost:5173/
